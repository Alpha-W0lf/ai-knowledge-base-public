# RAG hooks and retrieval stages

This synthetic transcript explains RAG hooks for a retrieval pipeline.

When you wire Claude Code hooks into a knowledge base demo, the important stages are:
identity, content hash, hybrid retrieval, rank fusion, and optional cross-encoder rerank.

Use hooks to validate that hybrid search really ran vector plus keyword legs before fusion.
Placeholder prose for fixture:rag-hooks-01.
